# Core runtime dependencies
requests>=2.28
beautifulsoup4>=4.11
jinja2>=3.1

# Optional: web interface (`seoaudit web`)
flask>=2.2

# Optional: PDF reports (`--formats pdf`). Requires system libs; skip if not needed.
# weasyprint>=59
