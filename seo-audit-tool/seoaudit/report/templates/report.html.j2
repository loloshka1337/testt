<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>SEO Audit — {{ report.domain }}</title>
<style>
  :root {
    --bg: #0f1115; --card: #181b22; --fg: #e7e9ee; --muted: #9aa3b2;
    --accent: #4f8cff; --border: #262b36;
    --critical: #ff5c6c; --high: #ff9f43; --medium: #ffd15c; --low: #6fcf97; --info: #6aa9ff;
  }
  @media (prefers-color-scheme: light) {
    :root { --bg:#f5f6f8; --card:#fff; --fg:#1a1c22; --muted:#5b6472; --border:#e3e6ec; }
  }
  * { box-sizing: border-box; }
  body { margin:0; font-family: -apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;
         background:var(--bg); color:var(--fg); line-height:1.5; }
  .wrap { max-width: 1100px; margin: 0 auto; padding: 32px 20px 80px; }
  h1 { font-size: 1.7rem; margin: 0 0 4px; }
  h2 { font-size: 1.25rem; margin: 40px 0 14px; border-bottom:1px solid var(--border); padding-bottom:8px; }
  .sub { color: var(--muted); margin: 0 0 24px; }
  .grid { display:grid; grid-template-columns: repeat(auto-fit,minmax(150px,1fr)); gap:14px; }
  .card { background:var(--card); border:1px solid var(--border); border-radius:12px; padding:16px; }
  .stat { font-size:1.9rem; font-weight:700; }
  .stat.small { font-size:1.3rem; }
  .label { color:var(--muted); font-size:.8rem; text-transform:uppercase; letter-spacing:.04em; }
  table { width:100%; border-collapse:collapse; font-size:.9rem; }
  th,td { text-align:left; padding:9px 10px; border-bottom:1px solid var(--border); vertical-align:top; }
  th { color:var(--muted); font-weight:600; font-size:.78rem; text-transform:uppercase; letter-spacing:.03em; }
  .badge { display:inline-block; padding:2px 8px; border-radius:999px; font-size:.72rem; font-weight:700;
           color:#111; }
  .b-critical{background:var(--critical);} .b-high{background:var(--high);} .b-medium{background:var(--medium);}
  .b-low{background:var(--low);} .b-info{background:var(--info);}
  .pill { display:inline-block; padding:2px 8px; border:1px solid var(--border); border-radius:6px;
          font-size:.75rem; color:var(--muted); margin:2px 4px 2px 0; }
  a { color:var(--accent); word-break:break-all; }
  .url { font-family: ui-monospace,SFMono-Regular,Menlo,monospace; font-size:.82rem; }
  details { background:var(--card); border:1px solid var(--border); border-radius:10px; margin:8px 0; padding:6px 12px; }
  summary { cursor:pointer; font-weight:600; }
  .muted{color:var(--muted);}
  .note { background:var(--card); border-left:3px solid var(--accent); border-radius:6px; padding:12px 14px; margin:14px 0; color:var(--muted); font-size:.86rem;}
  .bar { height:8px; border-radius:6px; background:var(--border); overflow:hidden; }
  .bar > span { display:block; height:100%; background:var(--accent); }
  code { background:rgba(127,127,127,.15); padding:1px 5px; border-radius:4px; font-size:.82rem; }
</style>
</head>
<body>
<div class="wrap">
  <h1>SEO Visibility Audit</h1>
  <p class="sub">
    Domain <strong>{{ report.domain }}</strong> ·
    generated {{ report.generated_at }} ·
    tool v{{ report.tool_version }}
  </p>

  <div class="note">
    This report covers only <strong>{{ report.domain }}</strong>. It is intended for the domain
    owner or an authorised auditor. Findings are advisory — verify before acting.
  </div>

  <h2>Summary</h2>
  <div class="grid">
    <div class="card"><div class="label">Dorks run</div><div class="stat">{{ report.dorks|length }}</div></div>
    <div class="card"><div class="label">URLs discovered</div><div class="stat">{{ report.results|length }}</div></div>
    <div class="card"><div class="label">Pages analysed</div><div class="stat">{{ total_pages }}</div></div>
    <div class="card"><div class="label">Indexable</div><div class="stat">{{ indexable }}</div></div>
    <div class="card"><div class="label">Not indexable</div><div class="stat">{{ not_indexable }}</div></div>
    <div class="card"><div class="label">With issues</div><div class="stat">{{ with_issues }}</div></div>
    <div class="card"><div class="label">Possible leaks</div><div class="stat" style="color:var(--critical)">{{ with_leaks }}</div></div>
    <div class="card"><div class="label">Recommendations</div><div class="stat">{{ report.recommendations|length }}</div></div>
  </div>

  <h2>Recommendations by action</h2>
  <div class="grid">
    {% for action, n in action_counter.most_common() %}
    <div class="card"><div class="label">{{ action }}</div><div class="stat small">{{ n }}</div></div>
    {% else %}
    <p class="muted">No recommendations generated.</p>
    {% endfor %}
  </div>

  {% if report.recommendations %}
  <h2>Prioritised recommendations</h2>
  <table>
    <thead><tr><th>Sev</th><th>Action</th><th>Target</th><th>Recommendation</th></tr></thead>
    <tbody>
    {% for r in report.recommendations %}
      <tr>
        <td><span class="badge b-{{ r.severity.value }}">{{ r.severity.value }}</span></td>
        <td><span class="pill">{{ r.action.value }}</span></td>
        <td class="url">
          {% if r.scope == 'page' %}<a href="{{ r.target }}">{{ r.target }}</a>{% else %}{{ r.target }}{% endif %}
        </td>
        <td><strong>{{ r.title }}</strong><br><span class="muted">{{ r.detail }}</span></td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
  {% endif %}

  <h2>Page clusters</h2>
  <table>
    <thead><tr><th>Cluster</th><th>Type</th><th>Pages</th></tr></thead>
    <tbody>
    {% for c in report.clusters %}
      <tr><td>{{ c.name }}</td><td><code>{{ c.page_type }}</code></td><td>{{ c.urls|length }}</td></tr>
    {% endfor %}
    </tbody>
  </table>

  {% if top_issues %}
  <h2>Most common issues</h2>
  <table>
    <thead><tr><th>Issue code</th><th>Count</th></tr></thead>
    <tbody>
    {% for code, n in top_issues %}
      <tr><td><code>{{ code }}</code></td><td>{{ n }}</td></tr>
    {% endfor %}
    </tbody>
  </table>
  {% endif %}

  <h2>Pages ({{ total_pages }})</h2>
  {% for a in report.analyses %}
  <details>
    <summary>
      {% if not a.indexable %}<span class="badge b-high">blocked</span>{% endif %}
      {% if a.leaks %}<span class="badge b-critical">leak</span>{% endif %}
      <span class="url">{{ a.url }}</span>
      <span class="muted">— {{ a.status_code or '—' }} · {{ a.word_count }} words</span>
    </summary>
    <div style="padding:8px 0">
      <span class="pill">status {{ a.status_code or 'n/a' }}</span>
      <span class="pill">indexable: {{ 'yes' if a.indexable else 'no' }}</span>
      <span class="pill">robots index: {{ a.robots_index }}</span>
      {% if a.canonical %}<span class="pill">canonical: {{ 'self' if a.canonical_is_self else a.canonical }}</span>{% endif %}
      {% if a.redirect_chain|length > 1 %}<span class="pill">{{ a.redirect_chain|length - 1 }} redirect(s)</span>{% endif %}
      {% if a.title %}<div style="margin-top:6px"><strong>{{ a.title }}</strong></div>{% endif %}
      {% if a.meta_description %}<div class="muted" style="font-size:.85rem">{{ a.meta_description }}</div>{% endif %}
      {% if a.issues %}
        <div style="margin-top:8px" class="label">Issues</div>
        {% for i in a.issues %}
          <div><span class="badge b-{{ i.severity.value }}">{{ i.severity.value }}</span>
            <code>{{ i.code }}</code> — {{ i.message }}</div>
        {% endfor %}
      {% endif %}
      {% if a.leaks %}
        <div style="margin-top:8px" class="label" style="color:var(--critical)">Possible leaks</div>
        {% for i in a.leaks %}
          <div><span class="badge b-{{ i.severity.value }}">{{ i.severity.value }}</span>
            <code>{{ i.code }}</code> — {{ i.message }}</div>
        {% endfor %}
      {% endif %}
    </div>
  </details>
  {% endfor %}

  <h2>Search queries used</h2>
  <table>
    <thead><tr><th>Category</th><th>Query</th><th>Why</th></tr></thead>
    <tbody>
    {% for d in report.dorks %}
      <tr><td><code>{{ d.category }}</code></td><td class="url">{{ d.query }}</td><td class="muted">{{ d.rationale }}</td></tr>
    {% endfor %}
    </tbody>
  </table>

  <p class="sub" style="margin-top:40px">
    Generated by seoaudit — a white-hat, single-domain SEO visibility auditor.
  </p>
</div>
</body>
</html>
